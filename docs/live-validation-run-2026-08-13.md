# 2026-08-13 Playwright 个人账号真实验收记录

- 状态：部分完成
- Git commit：`0c68350`
- 软件包版本：`0.3.3`
- 执行入口：源码目录 CLI
- 后端：仅 Playwright
- 明确排除：Public API 和真实打卡

## 自动化基线

| 检查 | 结果 |
| --- | --- |
| `npm test` | 通过，93/93 |
| `npm run validate` | 通过 |
| `npm run package:smoke` | 通过 |
| `git diff --check` | 通过 |

## Playwright 只读基线

- 后端状态：`playwright`，版本 `0.3.3`。
- 浏览器认证：已登录，凭据存储为 System Keychain。
- 本人申请：pending 0、returned 0、approved 40、all 40。
- 管理员 pending：0。
- 月次状态：`2026-08` 为 `unsubmitted`，可用操作为 `submit`。
- `2026-08-17` 和 `2026-08-18` 没有既有同日申请。
- 两个候选日期均提供 `有休`、`有休（半休）` 和 `夏季休暇（有給）`。

只记录了数量和状态，没有保存员工表格、凭据、Cookie、Token 或浏览器 profile 数据。

## 缺少确认拒绝

| 测试 | 结果 | 状态复查 |
| --- | --- | --- |
| 月次 `submit` commit 未提供确认 | `CONFIRMATION_REQUIRED` | `2026-08` 仍为 `unsubmitted` |
| 本人 `有休` 创建 commit 未提供确认 | `CONFIRMATION_REQUIRED` | 本人申请总数仍为 40 |

两个调用都在浏览器业务操作前被拒绝，没有写入 freee。

## 错误指纹拒绝

| 测试 | 结果 | 状态复查 |
| --- | --- | --- |
| `PA-SF-01`：为 `2026-08-17` 全日 `有休` 创建操作传入 64 位全零错误指纹 | `PERSONAL_APPLICATION_PREVIEW_CHANGED`；当前正确指纹前 12 位仍为 `ef49c786ff9b` | 本人申请总数仍为 40，目标日期没有新增申请 |

该调用获得了单独确认，并在点击 `申請` 前被指纹检查拒绝，没有写入 freee。

## 只读表单和日期变体

| 测试 | 结果 | 指纹前 12 位 |
| --- | --- | --- |
| `2026-08-17` 全日 `有休` | 预览匹配，审批路线存在 | `ef49c786ff9b` |
| `2026-08-18` `有休（半休）` | 预览匹配，审批路线存在 | `42b4a94ff614` |
| `2026-08-18` `夏季休暇（有給）` | 预览匹配，审批路线存在 | `1adff57ca23d` |
| `2026-09-01` 跨月 `有休` | 日期和类型匹配，审批路线存在 | `a3445fa9e335` |
| `2026-08-12` 勤務時間修正，无休憩 | `09:00`–`18:00` 匹配 | `841601112dff` |
| `2026-08-12` 勤務時間修正，一段休憩 | `09:00`–`18:00`、`12:00`–`13:00` 匹配 | `bb2105f7e8af` |

所有案例都只填写并验证表单，没有点击 `申請`。

部门日期边界验证：

- `2026-08-13` 与 freee 当前选中月份 `2026年8月` 一致，读取成功；只记录汇总数量为 9 人，其中 3 人存在勤怠问题。
- `2026-09-01` 与当前选中月份不一致，返回 `BROWSER_DATE_UNSUPPORTED`，没有自动切换月份。

## 尚待独立确认

以下案例尚未执行，每一个潜在写入都需要新的当前消息单独确认：

1. 其它操作族的错误指纹安全测试；
2. `PA-LW` 创建并由本人撤回；
3. `PA-LR` 创建并由管理员差戻し；
4. `PA-WT-A` 勤務時間修正及 `PA-WT-R` 准确恢复；
5. `PA-M` 月次提交及条件允许时的撤回；
6. Playwright 登录恢复和已安装 Agent 验收。

本次记录范围内没有执行任何 freee 写入。
