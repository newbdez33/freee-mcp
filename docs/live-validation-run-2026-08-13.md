# 2026-08-13 Playwright 个人账号真实验收记录

- 状态：部分完成
- Git commit：`0c68350`
- 软件包版本：`0.3.3`
- 执行入口：源码目录 CLI
- 后端：仅 Playwright
- 明确排除：Public API 和真实打卡

## 自动化基线

| 检查 | 结果 |
| --- | --- |
| `npm test` | 通过，93/93 |
| `npm run validate` | 通过 |
| `npm run package:smoke` | 通过 |
| `git diff --check` | 通过 |

## Playwright 只读基线

- 后端状态：`playwright`，版本 `0.3.3`。
- 浏览器认证：已登录，凭据存储为 System Keychain。
- 本人申请：pending 0、returned 0、approved 40、all 40。
- 管理员 pending：0。
- 月次状态：`2026-08` 为 `unsubmitted`，可用操作为 `submit`。
- `2026-08-17` 和 `2026-08-18` 没有既有同日申请。
- 两个候选日期均提供 `有休`、`有休（半休）` 和 `夏季休暇（有給）`。

只记录了数量和状态，没有保存员工表格、凭据、Cookie、Token 或浏览器 profile 数据。

## 缺少确认拒绝

| 测试 | 结果 | 状态复查 |
| --- | --- | --- |
| 月次 `submit` commit 未提供确认 | `CONFIRMATION_REQUIRED` | `2026-08` 仍为 `unsubmitted` |
| 本人 `有休` 创建 commit 未提供确认 | `CONFIRMATION_REQUIRED` | 本人申请总数仍为 40 |

两个调用都在浏览器业务操作前被拒绝，没有写入 freee。

## 错误指纹拒绝

| 测试 | 结果 | 状态复查 |
| --- | --- | --- |
| `PA-SF-01`：为 `2026-08-17` 全日 `有休` 创建操作传入 64 位全零错误指纹 | `PERSONAL_APPLICATION_PREVIEW_CHANGED`；当前正确指纹前 12 位仍为 `ef49c786ff9b` | 本人申请总数仍为 40，目标日期没有新增申请 |
| `PA-SF-02`：为 `2026-08` 月次提交操作传入 64 位全零错误指纹 | `MONTHLY_PREVIEW_CHANGED`；当前正确指纹前 12 位仍为 `ec25792167e4` | 月次状态仍为 `未申請`，没有对应申请 |

两个调用分别获得了单独确认，并在点击业务按钮前被指纹检查拒绝，没有写入 freee。

## 只读表单和日期变体

| 测试 | 结果 | 指纹前 12 位 |
| --- | --- | --- |
| `2026-08-17` 全日 `有休` | 预览匹配，审批路线存在 | `ef49c786ff9b` |
| `2026-08-18` `有休（半休）` | 预览匹配，审批路线存在 | `42b4a94ff614` |
| `2026-08-18` `夏季休暇（有給）` | 预览匹配，审批路线存在 | `1adff57ca23d` |
| `2026-09-01` 跨月 `有休` | 日期和类型匹配，审批路线存在 | `a3445fa9e335` |
| `2026-08-12` 勤務時間修正，无休憩 | `09:00`–`18:00` 匹配 | `841601112dff` |
| `2026-08-12` 勤務時間修正，一段休憩 | `09:00`–`18:00`、`12:00`–`13:00` 匹配 | `bb2105f7e8af` |

所有案例都只填写并验证表单，没有点击 `申請`。

部门日期边界验证：

- `2026-08-13` 与 freee 当前选中月份 `2026年8月` 一致，读取成功；只记录汇总数量为 9 人，其中 3 人存在勤怠问题。
- `2026-09-01` 与当前选中月份不一致，返回 `BROWSER_DATE_UNSUPPORTED`，没有自动切换月份。

## 尚待独立确认

以下案例尚未执行，每一个潜在写入都需要新的当前消息单独确认：

1. 其它操作族的错误指纹安全测试；
2. `PA-LW` 网页确认、问题修复，以及确认未创建后的重新创建和本人撤回；
3. `PA-LR` 创建并由管理员差戻し；
4. `PA-WT-A` 勤務時間修正及 `PA-WT-R` 准确恢复；
5. `PA-M` 月次提交及条件允许时的撤回；
6. Playwright 登录恢复和已安装 Agent 验收。

除下述结果不明的 `PA-LW` 创建尝试外，其余已执行案例均未点击 freee 业务按钮。

## `PA-LW` 创建结果不明

在单独确认后，使用以下预览执行了一次创建：

- 日期：`2026-08-14`；
- 类型：`有休（半休）`；
- 理由：`freee-mcp validation PA-LW 2026-08-14`；
- 指纹前 12 位：`d4c2661bb1c1`。

点击 `申請` 后返回 `PERSONAL_APPLICATION_ACTION_RESULT_UNKNOWN`。没有重试。之后的只读检查结果为：

- pending 仍为 0；
- all 和 approved 仍为 40；
- `2026-08-14` 和 `PA-LW` 均没有匹配记录；
- returned 筛选另行返回 `BROWSER_MONTHLY_PAGE_UNEXPECTED`，没有用于推断业务状态。

现有只读证据倾向于申请没有创建，但由于业务按钮已经被点击，本案例保持结果不明。没有自动重试；维护者随后明确要求在记录步骤截图并重新审核预览后再尝试一次。

### 提交前截图诊断和修复

经维护者明确要求，新增了默认关闭、仅在仓库外私有临时目录启用的步骤截图。没有进行第二次点击。截图显示：选择 `有休（半休）` 后，freee 会额外显示休假时间范围，而原实现保留了无效的默认值 `00:00–00:00`。这解释了第一次点击后没有可验证申请的现象。

实现新增 `leave_start` 和 `leave_end`，将两个时间写入预览和指纹；当表单显示时间范围但调用方未提供时，prepare 现在会停止。使用维护者提供的下午半休时段完成了新的只读验证：

省略时间的真实表单 prepare 已返回 `PERSONAL_APPLICATION_LEAVE_TIME_REQUIRED`，没有生成可提交预览，也没有点击 `申請`。

- 日期：`2026-08-14`；
- 类型：`有休（半休）`；
- 时间：`13:00–18:00`；
- 消化数：`0.5日`；
- 理由：`freee-mcp validation PA-LW 2026-08-14`；
- 审批路线：`勤怠申請`；
- 当前申请数：40；
- 新指纹前 12 位：`2e64d50ee9de`。

截图确认 freee 页面准确显示 `13:00–18:00`。该步骤只生成预览，没有点击 `申請`。

### 第二次创建及验证

维护者审核新预览后单独确认了第二次点击。操作过程保存了提交前、点击后和结果列表截图：

- 点击后 freee 显示 `申請が作成されました。`；
- 详情页显示 No. `10032`、状态 `申請中`、`有休 13:00–18:00（半休）`、消化数 `0.5日`；
- 申请列表总数由 40 变为 41，新记录位于第一行；
- CLI 执行通道没有返回 JSON，因此没有据此宣告成功；
- 后续独立只读列表和详情确认 pending 总数为 1，No. `10032` 字段完全匹配，且 `withdraw` 可用。

综合截图和独立读取，本次创建确认成功。随后只生成了撤回预览，没有点击撤回：

- 申请 No.：`10032`；
- 撤回指纹前 12 位：`93fc90c7e941`。

维护者单独确认撤回后，只执行了一次 `申請を取り下げる`。工具和独立读取均验证：

- No. `10032` 状态为 `差戻し`；
- pending 总数恢复为 0；
- returned 总数为 1，且唯一记录为 No. `10032`；
- 同一记录仍准确显示 `2026-08-14`、`有休 13:00〜18:00（半休）` 和测试理由；
- 详情不再提供 `withdraw`。

`PA-LW` 创建、读取和本人撤回生命周期验证通过。撤回保留了工作流历史，没有删除申请。
